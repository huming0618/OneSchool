(function(model){
	model.selected = Backbone.Model.extend({
		defaults: {
            model: '',
            _blog : ''
        },
        initialize: function(){
        	
        }
    });
}(window.zgwApp.model))
